name: Actualizar Eventos Almansa

on:
  schedule:
    # Ejecutar cada domingo a las 12:00 UTC
    - cron: '0 12 * * 0'
  
  workflow_dispatch: # Permite ejecución manual

jobs:
  extraer-eventos:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout del repositorio
        uses: actions/checkout@v4
      
      - name: 🐍 Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 Instalar dependencias
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: 🕷️ Extraer eventos de las fuentes
        run: |
          cd scripts
          python3 extractor_eventos_v3.py
          cp eventos_agenda.json ../eventos_agenda.json
          echo "✅ JSON copiado a la raíz del repositorio"
      
      - name: 📤 Commit y Push de eventos_agenda.json
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git add eventos_agenda.json
          git add scripts/eventos_agenda.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "🔄 Actualización automática de eventos - $(date +'%Y-%m-%d %H:%M')" && git push)
```

4. **Commit changes**:
   - Mensaje: `🔧 Fix: Copiar JSON a raíz y añadir ambos archivos`
   - Click **Commit changes**

---

## ⚠️ BONUS: Arreglar Almansa Cultura (Error 404)

También veo que Almansa Cultura sigue dando error 404:
```
🔍 Extrayendo eventos de Almansa Cultura...
⚠️ Error HTTP 404
